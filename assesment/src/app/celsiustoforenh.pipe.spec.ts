import { CelsiustoforenhPipe } from './celsiustoforenh.pipe';

describe('CelsiustoforenhPipe', () => {
  it('create an instance', () => {
    const pipe = new CelsiustoforenhPipe();
    expect(pipe).toBeTruthy();
  });
});
